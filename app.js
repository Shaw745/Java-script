 // revision
 // functions- functions declaration and arrow functions
// for function to be useful, it has to return a value 
// 2- template literals
 let user = "isaac";
 let welcomeMessage = `welcome ${user}`
// 3- short conditional && || 
false && <p> hello</p>
let issuerLoggedin = true;
issuerLoggedin && <h1>welcome user</h1>

false || true || false;

// ternary operator
 false ? "hello" : "goodbye";

 issuerLoggedin ? " welcome user " : "log out user";

 let isError = false;
 let isLoading = true;
 let data = 'data from API';

 isError && <p>Error loading the page</p>;
 isLoading && <P> loading...</P>;
 data && <p>data</p>
 // customer.length === 0 && <p> 
 // array methods: .map(), filter(), reduce() find()
 // object tricks: property shoertaand , destructuring, speed operator 
 //  promises + async/ await syntax 

 